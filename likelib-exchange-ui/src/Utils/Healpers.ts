export const maximum = (num1: number, num2: number) => {
  if (num1 > num2) return num1;
  return num2;
};

export const minimum = (num1: number, num2: number) => {
  if (num1 > num2) return num2;
  else return num1;
};
